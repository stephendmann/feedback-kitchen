# Environment & secrets
.env
.env.*
!.env.example
!.env.local.example
secrets.*
credentials.*

# Vercel
.vercel

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Safety nets — files with these naming patterns
*not*upload*
*do*not*commit*
*private*
*sensitive*

# Files staged for manual deletion (sandbox cannot rm; remove from your shell)
_todelete/

# One-off timestamped backup files (use git history instead)
scorer.backup.*.html
*.backup.*.html

# Recovery artefacts from Claude Code restore operations
*.restored

# Brand-film UI capture output (regenerable, large PNG/MP4)
capture-out/

# Graphify generated output (knowledge graph cache, analysis, reports)
graphify-out/

# Claude Code local settings (machine-specific paths, session IDs)
.claude/settings.local.json

# Real Moodle exports. These carry live student names, institutional IDs and
# email addresses, and this repo is public. Moodle names them Grades-<course>-
# <assignment>-<cmid>.csv; keep them outside the repo entirely. The
# guard:student-data check is the backstop if one is force-added anyway.
Grades-*.csv
*grading-worksheet*.csv
