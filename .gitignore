# Environment & secrets
.env
.env.*
!.env.example
!.env.local.example
secrets.*
credentials.*

# Vercel
.vercel

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Safety nets — files with these naming patterns
*not*upload*
*do*not*commit*
*private*
*sensitive*

# Files staged for manual deletion (sandbox cannot rm; remove from your shell)
_todelete/

# One-off timestamped backup files (use git history instead)
scorer.backup.*.html
*.backup.*.html
