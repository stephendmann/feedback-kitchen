# Real student data — never commit, never push (INS-10 source artefact).
# Structural findings live in planning/INSPECTION.md; delete or anonymise
# this CSV once FK-19's controlled-upload test has its fake-data replacement.
*.csv
