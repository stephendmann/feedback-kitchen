# fix/aria-rubric-table

Implementation branch for PR 4 of the Feedback Kitchen roadmap.

## Scope
- `scorer.html` only (~25 lines changed)
- Rubric table accessibility attributes

## Changes
- Table header cells: add `scope="col"` (and `scope="row"` for row headers)
- Grade `<select>` elements generated by `renderCriteriaRows`: add `aria-label="Grade for [criterion name]"`
- Grade override `<input>`: wire `aria-invalid="true"` when value is out-of-range; `aria-invalid="false"` when valid
- Table caption or `aria-label` on the `<table>` element itself

## Key touch-points (from CC parallel-reader pass)
- `renderCriteriaRows` — the primary generation point for table rows and selects
- Table headers — enumerated by parallel-reader pass
- `#grade-override` input — aria-invalid wiring

## Sequencing note
Merge AFTER PR 3 (fix/accessible-modals) to avoid touching the same render
functions twice in parallel.

## See also
ROADMAP.md — PR Sequence table, PR 4
